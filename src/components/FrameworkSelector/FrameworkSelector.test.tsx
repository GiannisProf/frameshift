import { describe, it } from "vitest";

describe("FrameworkSelector", () => {
  it.todo("renders source and target selects");
  it.todo("shows auto-detected badge when framework is auto-detected");
  it.todo("updates source framework on selection");
});
