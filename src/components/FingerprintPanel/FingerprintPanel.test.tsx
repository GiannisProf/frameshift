import { describe, it } from "vitest";

describe("FingerprintPanel", () => {
  it.todo("toggle is disabled when no translation exists");
  it.todo("shows empty state when fingerprint is empty");
  it.todo("shows concept cards when fingerprint has items");
  it.todo("toggles open/closed on button click");
});
