import type { Framework } from "../types";

export const SYSTEM_PROMPT = "";

export function buildUserMessage(
  _sourceFramework: Framework,
  _targetFramework: Framework,
  _sourceCode: string,
): string {
  return "";
}
